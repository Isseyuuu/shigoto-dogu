# CLAUDE.md

このリポジトリで作業する Claude Code / Codex への指示。

## 体制

- **Manager = オーナー(人間)**。方針・ターゲット層・収益判断はすべて Manager が決める。
  エージェントが「初心者には高すぎる」等の理由で勝手に対象を絞ってはいけない
  (実際に価格上限を独断で設けて指摘された)。
- **Codex** = 制作担当。HTML/CSS、デザイン、画像生成、記事執筆、構造化データ。
- **Claude Code** = 検証と門番担当。Web裏取り、公開可否の判定、push。
  Codex はWeb取得ができないため、外部の事実(価格・規約・法令・仕様)は
  必ず Claude Code が実データで確認してから公開する。

互いの得意分野を尊重する。相手ができないと決めつける前に、実績と事実を確認すること
(Codex に画像生成能力が無いと誤って断定した事故がある)。

### Codex への指示は stdin で渡す(最重要・複数の失敗の真因)

**`codex exec` に複数行の文字列を引数として渡すと、最初の1行で切り捨てられる。**
Codex が実測で確認した。

```
送信: 行1 / 行2:ALPHA7を復唱 / 行3:BETA9を復唱
Codex が受信した内容: 「行1」のみ
```

`type prompt.txt | codex exec ... -` のように stdin 経由にすると全文が届く
(ALPHA7/BETA9 とも復唱された)。

この切り捨てが、次の失敗の真因だった可能性が高い。

- Codex が「imagegen Skill が存在しない」と報告した件。Skill を探せという指示も
  含めて本文が届いておらず、1行目だけで判断していた。
- 生成画像が主題・配色・アスペクト比・保存先を同時に外した件。ブリーフが
  届いていなかった。

**Codex 側の判断力の問題ではなく、伝達経路の欠陥だった。** 報告が不可解なときは、
まず「指示が届いているか」を疑うこと。届いていない指示に対する応答を、
能力や判断の問題と解釈してはいけない。

長文を渡すときは必ずファイルに書き出して stdin で流す。


### Codex の画像生成能力（Manager 確認済み）

現在の Codex 環境には、画像生成・画像編集用の **`imagegen` Skill** と
**`image_gen.imagegen` 実行ツール**がある。Codex は新規のビットマップ画像を生成し、
既存画像を指示に沿って編集し、生成物をサイトへ組み込める。

OpenAI の公式ユースケースにも、Codex で ImageGen を使って構想を視覚化し、
フロントエンドの試作を作るワークフローが掲載されている:
https://developers.openai.com/codex/use-cases

Claude Code は、画像が必要な作業を「Codex には生成できない」という理由で却下せず、
次の情報を添えて Codex に制作を依頼する。

- 画像の用途と掲載ページ
- 希望する構図・雰囲気・縦横比
- 実在ブランド、商標、人物、施設など避ける対象
- 保存先と必要な代替テキスト

Codex は、生成後にファイルパス、寸法、用途、目視確認結果、代替テキスト案、
ブランドや事実の誤認リスクを Claude Code へ返す。Claude Code は公開前の門番として、
サイト方針との整合、表示、権利・誤認リスクを確認する。
#### 実環境の所在(確認済み・ここを間違えて2度失敗した)

- Skill の実体は **`C:\Users\20008\.codex\skills\.system\imagegen\`**。
  `$CODEX_HOME` は `C:\Users\20008\.codex`。
- **`~/.claude/skills/` を探してはいけない。** そこは Claude Code の領域であり
  Codex のものではない。実際に Codex が `.claude` 配下を探して「Skill が存在しない」と
  報告し、Claude Code もそれを裏取りせず追認して、Manager に二度「できない」と
  誤報告した。**製品が違えば設定の置き場所も違う。**
- 生成物は既定で **`$CODEX_HOME/generated_images/...` に保存される**。
  SKILL.md は「プロジェクトが参照するアセットをそこに置いたままにしてはいけない」と
  定めており、**生成後にワークスペースへ移動する手順を指示に含めること**。
  これを欠いたため、生成は成功したのに画像がリポジトリに入らない失敗が起きた。
- 既定は built-in の `image_gen` ツール(`OPENAI_API_KEY` 不要)。
  CLI フォールバック(`scripts/image_gen.py`)は Manager が明示的に求めた時だけ。
- 既存アセットの上書きは Manager が明示的に置き換えを求めた場合のみ。
  そうでなければ `hero-v2.png` のように別名で作る。

#### 生成を依頼するときの実務

生成は時間とコストがかかる。1枚目で主題・配色・アスペクト比・保存先を同時に外した
実例があるため、依頼時に次を含める。

- **1枚ずつ生成し、都度 Codex 自身が目視確認**してから次へ進む
- アスペクト比と配色をプロンプト内の明示的な制約として書く
- **再試行の上限回数を指示に書く**(例: 1枚あたり最大2回)。無制限に回すと費用が嵩む
- CSS 側で `filter: saturate()` 等がかかる場合、その上から表示される前提で
  元画像の彩度を決めるよう伝える


## 記事の分割原則(Manager 指示)

**細かなジャンルが異なるものは、基本的に別記事にする。**

狙う読者層と検索意図が違うものを1記事にまとめない。例:
- 登山とトレッキングは別記事
- 婚約指輪のブランド比較、式場の探し方、プランの立て方はそれぞれ別記事
- ランニングとHYROXとピックルボールは別記事

由来: 仕事道具(会計)と運動道具を1サイトに同居させた結果、サイトの主題が
曖昧になり、収益導線を入れた記事群が noindex でアーカイブされるという
食い違いが起きた。テーマが違うものは記事どころかサイトから分ける。

## サイト構成

- **shigoto-dogu**(このリポジトリ) — 仕事道具(会計・確定申告・業務ツール)と
  ブライダル(婚約指輪・式場・プラン)。物販アフィリエイトはこちらで行う。
- **yama-hajime**(別リポジトリ) — 登山・トレッキング・スイミング。
  デザインは「雨の森林」。落ち着いたトーン。
- 総合商社の実態に関する有料コンテンツは別途 Manager が進める(このリポジトリ対象外)。

note.com は **Amazonアソシエイト以外のアフィリエイトが使えない**(楽天・A8のリンクは
システム側で弾かれ記事を公開できない)。調査済み。物販はサイト側で行う。

## 執筆トーン

**書き手は「中級者」**という前提で書く。専門家の権威でも初心者の体当たりでもなく、
少し先を行く経験者が初心者に向けて書く。実体験に基づく具体性は出すが、断定しすぎない。
Manager 曰く「これが一番読者に刺さる」。

## 絶対に守る制約

### Codex は外部の事実を書かない

Codex はWeb取得ができない。価格・料金相場・キャンペーン期限・法令の数値・製品スペック・
施設情報を**本文に書いてはいけない**。必要な箇所は次の形式で印を付け、本文には書かない。

```html
<!-- FACTCHECK: 確認が必要な内容 -->
```

Claude Code が Web で裏取りしてから埋める。

由来: Web取得ができない状態で書かれた会計ソフトの料金と税制改正の数値は、
検証したところ結果的に正確だった。しかし「たまたま正しかったこと」と
「構造的に検証できないこと」は別問題であり、裏取りの分担は維持する。

### 広告表記

- リンクには `rel="sponsored nofollow"` を付ける。
- ボタン付近に「広告」を明示する。
- レビュー件数・平均評価を載せる場合は「楽天市場の集計値」と出典を明記し、
  **当サイトの実測と読める書き方をしない**。
- 報酬額を理由に事実と異なる順位を付けない(about.html の編集方針)。

### ダミーリンクを公開状態に置かない

実URLが確定するまで、広告枠は HTML コメントで封じる。

```html
<!-- RAKUTEN_AD_SLOT: <slot名> ... -->  ... 中身 ...  <!-- /RAKUTEN_AD_SLOT -->
```

`href="#"` のボタンを公開すると、踏んでも何も起きず読者の信用を損なう。
マーカーは描画後も残すこと。`scripts/render_ad_slot.py` を再実行するだけで
価格・レビュー件数を更新できる設計になっている(手で貼り直すと更新漏れが起きる)。

### 画像

- ブランドの実物・ロゴ・商標に見える画像を生成しない(GRAFF/カルティエ/
  ハリー・ウィンストン等)。抽象的・象徴的なイメージに留める。
- 実在の特定施設・特定の山と誤認される画像を避ける。
- 実物写真は必須ではない。イメージ画像と比較の図で成立させる。

### 安全に関わる記述

登山・水泳など事故が命に関わる領域では「これで大丈夫」「安全です」と読める
表現をしない。公的機関・競技団体の情報を確認するよう促す構造にする。

## 楽天API

```powershell
# ジャンルIDの確定(推測で決めない。階層を降りて名前を目視確認する)
python scripts/rakuten_genre.py 0 --find スポーツ

# 商品とレビュー集計の取得
python scripts/rakuten_fetch.py --genre 565768 --pages 4 --min-reviews 50 --out data/running_shoes.json

# 掲載候補の選定(ブランド限定 + 下限)
python scripts/select_items.py

# 記事の広告枠へ描画(再実行で更新可能)
python scripts/render_ad_slot.py --dry-run
```

認証情報は `.env`(`RAKUTEN_APP_ID` / `RAKUTEN_ACCESS_KEY` / `RAKUTEN_AFFILIATE_ID`)。
**2026年版APIは applicationId と accessKey の両方が必須**。`.env` は絶対にコミットしない。

### 楽天APIの落とし穴(実データで確認済み)

- **キーワード検索は使えない**。「ランニングシューズ メンズ」で検索すると
  レビュー件数上位が靴紐・インソール・格安スニーカーで埋まる。ジャンル指定を使う。
- **ジャンル指定でもノイズが残る**。「ランニング>シューズ」にトレーニング向けや
  ライフスタイル系列が混入する。除外語での絞り込みが要る。
- **itemPrice は税込とは限らない**。商品ごとの `taxFlag`(0=税込/1=税抜)で決まる。
  `postageFlag`(0=送料込/1=送料別)も同様。決め打ちすると誤表示になる。
- **商品名に販促文が入る**。「【期間限定 23%off】」等の期限付き文言をそのまま
  載せると、期限切れ後に嘘の表示になる。除去してから掲載する。
- **同一モデルが別ショップで重複する**。型番と派生語(メンズ/ワイド等)を無視して畳む。
- ジャンルAPIはパスが `ichibams` ではなく `ichibagt`。2026年版はレスポンス形式も
  旧版と異なる(`children` がフラット配列、名称キーは `nameJa`)。旧サンプルを流用しない。
- **IP許可リストがある**。アプリ登録時に登録したIP以外からは拒否される。
  家庭回線のグローバルIPは変わるため、401/403 が出たらまずIPを疑う。

## 公開前チェック

push する前に Claude Code が確認する。

1. `href="#"` のダミーリンクが公開状態に無いか
2. アフィリエイトリンクが実際に転送されるか、画像が配信されるか
3. HTMLコメントの開閉が一致しているか(コメント内に `--` があると早期終了して枠が露出する)
4. JSON-LD が構文として妥当か
5. 内部リンク切れが無いか、sitemap が実在ページと一致しているか
6. Codex が書いた外部事実に FACTCHECK の未処理が残っていないか
